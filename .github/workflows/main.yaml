name: Sanity check
on: [pull_request]

jobs:
  dco_check_job:
    runs-on: ubuntu-latest
    name: DCO Check
    steps:
    - name: DCO Check
      uses: Tim-Zhang/dco@master
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
